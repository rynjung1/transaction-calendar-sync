# Transaction Calendar Sync

*Syncs bank transactions to the user's calendar as timestamped, location-tagged spending events.*

**Repo**: https://github.com/rynjung1/transaction-calendar-sync

## What this app does
Connects to the user's bank account via Plaid, pulls transaction data (merchant, amount, timestamp, category), and writes each transaction as an event on the user's device calendar (whichever calendar they choose — Google, iCloud, or local — via the native calendar API). The goal: a passive spending diary that shows up automatically on the calendar you already check.

## Target platforms
- iOS only. Was originally iOS + Android; changed in hindsight — user doesn't have an Android device to test on. Android config in `mobile/app.config.ts`/`eas.json` is left in place (permissions, adaptive icon assets, build profile) as harmless dead weight rather than stripped out, in case Android gets picked back up later — don't build or test against it as an active target, and don't invest further effort keeping its config current.
- Built with Expo (React Native) + a custom dev client, using EAS Build for App Store submission. Not compatible with Expo Go — Plaid's SDK requires native modules.

## Confirmed architecture (do not deviate without discussing first)

### Frontend — `mobile/`
- Expo (React Native), TypeScript
- `react-native-plaid-link-sdk` v13+ (session-based API: `createPlaidLinkSession`, not the legacy `PlaidLink` component)
- `react-native-calendar-events` for writing to the device calendar (wraps EventKit on iOS, CalendarProvider on Android) — this is the ONLY calendar integration needed, do not build separate Google Calendar API / EventKit integrations, this library covers both
- App lets the user pick which on-device calendar to write to (calendar selection is handled by `findCalendars()` from this library)
- Android `minSdkVersion` is pinned to 26 via `expo-build-properties` (`mobile/app.config.ts`) — Plaid's Android SDK (`com.plaid.link:sdk-core`) requires it and fails the manifest merger at Expo's default of 24. Don't lower this without checking Plaid's SDK requirement first. ~~iOS deployment target hasn't been checked against Plaid's iOS SDK's own minimum~~ — resolved (see Mobile security findings): no explicit override is set, so it falls through to React Native 0.86.3's own Podfile default of `15.1`, which exactly matches Plaid's podspec minimum (`ios 15.1`). No conflict.
- **Runtime-verified for real, New Architecture risk resolved**: `expo-doctor` flagged `react-native-calendar-events` and `react-native-plaid-link-sdk` as untested against New Architecture (the only architecture available at RN 0.86.3/Expo 57 — not a toggle). Ran the actual end-to-end path on a fresh EAS development build on Simulator: sign up → sign in → Plaid Link (native SDK UI rendered, institution search showed real Canadian banks — RBC, Scotiabank, TD, BMO, CIBC, Tangerine, Desjardins, National Bank, Vancity, ATB — confirming Plaid's Canadian coverage) → sandbox institution login → `exchange-token` → calendar picker (`findCalendars()` found the Simulator's calendar) → confirmation dialog → `/api/plaid/sync` pulled 48 real sandbox transactions → all 48 written as calendar events, app UI showing "Added to calendar" per transaction. **Independently verified below the app layer**, not just trusting the UI: queried the Simulator's actual EventKit database directly (`~/Library/Developer/CoreSimulator/Devices/<udid>/data/Library/Calendar/Calendar.sqlitedb`) — confirmed exactly 48 rows in the target calendar (`DEFAULT_CALENDAR_NAME`), zero duplicates, zero missing; spot-checked event content matches the design exactly: `summary` = `"Starbucks — $4.33"` (merchant — amount format), `description` = `"FOOD_AND_DRINK"` (PFC category in notes, as decided), dates matching the sandbox data. Both libraries are confirmed working under New Architecture; this specific risk is closed.
- **Real bug hit and diagnosed during this test, not fully root-caused**: right after sign-in, `/api/plaid/status` returned 401 `"Invalid or expired session"` a few times, then succeeded on retry with the exact same persisted session and no code change. Isolated via direct curl against Supabase and the backend (both worked fine in isolation with a freshly-issued token), so the backend's `requireUser()` validation itself is not broken. Leading theory: the test account's email was confirmed via the Supabase admin API (`email_confirm: true`) rather than a real confirmation link, and that had a few minutes of propagation lag — a side effect of the test shortcut, not necessarily something a real user (who confirms via the actual email link) would hit. Not fully proven, so not marked fixed — worth adding defensive retry-on-401 in `authedFetch` regardless, since transient auth propagation lag could plausibly happen for real users too (e.g., right after a password reset).

### Backend — `backend/`
- Vercel serverless functions (TypeScript, `@vercel/node`)
- Responsibilities ONLY:
  1. Create Plaid `link_token` (must happen server-side, never in the client)
  2. Exchange `public_token` for `access_token` after Link success, store `access_token` encrypted
  3. Receive Plaid webhooks (`SYNC_UPDATES_AVAILABLE`, `ITEM_LOGIN_REQUIRED`, `ITEM_ERROR`)
  4. Call Plaid's Transactions Sync endpoint when webhook fires (or on a user-initiated refresh), return new transactions to client
- Do NOT poll Plaid on a timer — use the webhook + sync pattern (`backend/lib/plaidSync.ts`)
- `backend/api/plaid/webhook.ts` is the one endpoint that can't use `requireUser()` (Plaid has no user session) — it verifies Plaid's `Plaid-Verification` JWT instead (`backend/lib/plaidWebhookVerify.ts`: signature against Plaid's published key, `iat` freshness, and a body-hash check against the actual raw bytes received). A request that fails this check is rejected outright (401/400), not acked with 200 — unlike genuine processing errors after a request passes verification, which still return 200 to avoid Plaid's retry storms. This requires real `PLAID_CLIENT_ID`/`PLAID_SECRET` at runtime, since key lookup is a real Plaid API call.

### Backend resilience (findings from a dedicated failure-mode audit — "make sure it's actually functional and won't fail")
- **Structural gap, closed for the pure-logic half**: through this whole session, dozens of real, previously-broken behaviors were verified with one-off scripts that were run once and deleted — none of it was preserved as an automated regression test, so any future change could silently reintroduce any of them with zero warning. Added Vitest (`backend/vitest.config.mts`, `npm test`) and ported the DB-independent logic into real tests under `backend/test/` (mirrors `lib/`/`api/`'s structure, 38 tests across 7 files): `safeErrorInfo` (the actual secret-leak fix — proved the test really catches it by deliberately reverting the fix and watching the real secret show up in the test's own failure output, then restoring it), `shouldSyncTransaction` (amount/category filtering, including the legacy-taxonomy fallback), `validateSyncFilters`, `isValidMonth`/`monthRange`/`previousMonth` (extracted from `monthly.ts` for testability, matching this file's own established pattern), the crypto round-trip (including a GCM-tamper-detection case), and an integrity check on the full legacy category mapping (every one of its 176 entries actually maps to a real, current PFC value). **Deliberately out of scope for now**: the DB-dependent behavior that was actually the most load-bearing this session (`applyRemoved`/`applyModified`, the cursor compare-and-swap, webhook handling) — these were verified against the real Supabase test database and real Plaid sandbox webhooks by design, and turning that into a repeatable suite needs a real test-database/fixture strategy decided on its own, not bolted on as an afterthought.
- **Real deployment risk, caught before it shipped**: two of the new test files were first written co-located under `api/` (next to the code they test, e.g. `api/transactions/monthly.test.ts`) — checked Vercel's actual documented behavior rather than assuming, and confirmed every file under `api/` becomes a live serverless function route by default, with no built-in exclusion for `*.test.ts` (only an underscore-prefix convention, or moving the file out of `api/` entirely). Moved all test files into `backend/test/` (mirroring the source layout) before ever committing or deploying, so nothing under `api/` is anything but a real, intended route.
- **Real cost exposure, fixed — DB-based cooldown, no new external service**: confirmed no rate limiting existed anywhere in the backend (grepped for it, checked for a `vercel.json`, found nothing). `/api/plaid/sync` triggers real Plaid `transactionsSync` API calls, billed per-call — any authenticated user (or a stolen session token, or a future client-side retry-loop bug) could call it as fast as the network allowed with zero throttling, and the mobile pagination loop added earlier this session made this worse in practice: paging through one large pending backlog re-hit Plaid on every single page even though nothing new could plausibly have arrived between them. Fixed with a DB-based cooldown rather than a new external service (Redis/Upstash — the same shape of ask as Sentry/Turnstile, which the user was wary of adding more of): a new `plaid_items.last_synced_at` column (migration `0005_add_last_synced_at.sql`) and `isWithinCooldown()` (`backend/lib/plaidSync.ts`, pure and unit-tested — including the exactly-at-the-boundary case, which a naive `<=` would get backwards) gate whether `sync.ts` actually calls `syncPlaidItem` for a given item; a call inside the 30s window still returns the current pending page from our own DB (pagination and accidental double-taps stay fully functional), it just skips the redundant Plaid call. Stamped only on a *successful* sync, not before attempting one, so a transient Plaid failure never costs the user their next retry window. Deliberately scoped to this one endpoint — Plaid's own `SYNC_UPDATES_AVAILABLE` webhook always calls `syncPlaidItem` directly and is never subject to it, since Plaid controls that cadence, not an attacker-reachable client. **Deploy-order risk caught and closed before shipping, not assumed safe**: selecting a column that doesn't exist yet (this migration isn't applied to production as of this fix) would otherwise 500 the *entire* endpoint for every user the moment this code deploys. Probed the real failure shape against production rather than guessing — confirmed Postgres's own `42703` (`undefined_column`) SQLSTATE comes through `supabase-js`'s `error.code` verbatim — and `sync.ts` now detects exactly that code and falls back to the pre-migration query with the cooldown simply inactive, so this code is safe to deploy in either order; the protection turns on by itself the moment `0005` is actually run. **Migration `0005` not yet applied** — needs the same manual Supabase SQL Editor step as the still-pending `0004`.
- **Real bug, fixed and verified with genuine Plaid-signed sandbox webhooks, not just code review**: `webhook.ts` only ever handled 2 of Plaid's 8 documented `ITEM` webhook codes (`ERROR`, `LOGIN_REPAIRED`) — confirmed the full list by reading Plaid's own Items API docs. Missing `USER_PERMISSION_REVOKED`/`USER_ACCOUNT_REVOKED` (the user revokes access from their bank's own side, not through this app) meant an Item could be completely dead at Plaid's end while `plaid_items.status` stayed `"active"` forever: `/api/plaid/status` kept reporting the connection as healthy (`needsReauth` never fires for a still-"active" item), and `/api/plaid/sync` kept trying to sync a permanently-revoked Item on every call indefinitely. Fixed by mapping both codes to the existing `"error"` status — reuses `needsReauth`'s existing "not active" check with no schema change, and correctly points the user at the same reconnect path `login_required` already does. Verified for real: fired genuine Plaid-signed sandbox webhooks via `/sandbox/item/fire_webhook` for both codes against the real production endpoint and confirmed `status` flipped to `"error"` each time, then restored cleanly. Plaid's own docs additionally recommend deleting stored data associated with the Item for these two codes specifically (the access token is already dead at Plaid's end) — **now done**: migration `0006_nullable_revoked_access_token.sql` drops the column's `NOT NULL` constraint (deleting the `plaid_items` row outright was never the right call — it would cascade-delete the user's own transaction/Insights history over a bank-side event they didn't cause), and `webhook.ts` nulls `access_token_encrypted` alongside setting `status: "error"` for exactly these two codes. **Deploy-order risk caught and closed before shipping**: migration `0006` isn't applied to production yet, and probing the real column against a real row confirmed a combined update that nulls it fails outright with Postgres's `23502` (not-null-violation) — which, being one UPDATE statement, would have silently cost the already-working `status: "error"` half too. `webhook.ts` now falls back to a status-only update on exactly that error code. Verified live end-to-end, not assumed: fired a genuine Plaid-signed sandbox webhook against a real production item, confirmed `status` flipped to `"error"` (the fallback path engaging correctly) with the token left fully intact (expected, since `0006` isn't applied), confirmed a clean `200`/empty-`logs` Vercel log entry for that exact request, then restored the item to `"active"` with its token verified unchanged.
- Also confirmed and verified live: `PENDING_DISCONNECT` (US/CA-specific — this project's actual market, a 7-day advance warning before an Item stops working) is now logged (`console.warn`, confirmed appearing in real Vercel logs after firing a real sandbox webhook) rather than fully silent, but deliberately does not change `status` — doing so would stop `sync.ts` from syncing an Item that still works fine for up to 7 more days (it only syncs `status="active"` items), trading a real, avoidable transaction-history gap for an early warning that has nowhere to surface anyway (no UI currently distinguishes "about to need reconnecting" from "already broken"). A real fix needs a genuine schema addition (a distinct status, or a separate needs-attention-by timestamp) to warn without also halting sync — not implemented, flagged rather than half-built.
- **Critical real bug, partially fixed — the common case is closed, a narrower gap remains and is logged rather than silent**: `syncPlaidItem` only ever read `transactionsSync`'s `added` array; `modified` and `removed` (confirmed real fields on the SDK's own `TransactionsSyncResponse` type) were silently discarded entirely. Plaid's documented pattern for an ordinary pending→posted transition — the normal lifecycle of most card transactions, not an edge case — delivers the pending transaction's id in `removed` and the posted version in `added` under a brand-new `transaction_id`; since the two don't collide, this created a genuine second calendar event for the same real-world purchase whenever the pending version had already been synced. Fixed for the common case: a transaction still `"pending"` in our own table (never yet handed to mobile to write to a device calendar) is now cleanly deleted on `removed` or updated in place on `modified` — zero user-visible effect either way, since nothing was ever shown to the user for it yet. Verified against the real test database (not a hand-copied reimplementation) with 4 cases: pending+removed → deleted; synced+removed → preserved (can't be silently discarded, the device event still exists); pending+modified → fields updated in place; modified with no matching row → safe no-op. **Not fixed**: a transaction already synced to a real calendar event that Plaid later modifies or removes (a posted charge disputed/reversed, a settlement amount correction) — the device-side event can't be touched from the backend alone without mobile-side support (updating/deleting via `react-native-calendar-events`) that doesn't exist yet. Now logged loudly (`console.warn`) when this happens instead of silently doing nothing, so the gap is observable — a real follow-up feature, not something to silently claim as fully solved.
- **Real bug, fixed**: the cursor update on `plaid_items` was a blind write — two concurrent syncs for the same item (a webhook redelivery racing a pull-to-refresh, or racing itself) each read their own starting cursor independently and both wrote their own progress at the end with no check; whichever finished last won, even having advanced less far than a faster concurrent pass. Fixed with a real compare-and-swap (`UPDATE ... WHERE id = ? AND cursor = ?`, checking affected-row count) — at most one of two racing writers can advance the cursor per page; the other gets zero affected rows and aborts cleanly instead of silently clobbering progress. Verified against the real database: a normal single-writer advance succeeds; a second writer using a now-stale previous-cursor value correctly conflicts and throws rather than corrupting the stored value.
- **Real bug, fixed and corroborated independently by two separate audit passes**: `syncPlaidItem` buffered every page of a multi-page `transactionsSync` pull in memory and only persisted the advanced cursor once, after the *entire* pull and its DB upsert both completed. A large initial sync (Plaid can backfill up to 24 months of history on first link) can span many pages; any interruption partway through — a thrown error, or simply the function exceeding its execution time limit — discarded every already-fetched page's progress, and the next attempt restarted the identical backlog from scratch with no guarantee of ever converging if the full pull consistently takes longer than one invocation is allowed to run. Fixed: the cursor is now checkpointed after each individual page's `added`/`modified`/`removed` are all durably persisted, not once at the very end — an interruption now only ever costs the one page in flight. Verified live in production: ran a real sync through the rewritten loop and confirmed both a correct response and an intact, valid stored cursor afterward.
- **Real bug, fixed**: `PLAID_ENV` was read with a fallback (`?? "sandbox"`) but never validated against the SDK's actual supported values — confirmed by reading the installed SDK source that this version's `PlaidEnvironments` only defines `production` and `sandbox`; Plaid retired the old `development` environment, a name still common in older guides. Setting `PLAID_ENV=development` would have silently produced `basePath: undefined` with no startup error, surfacing only as a confusing low-level HTTP error on the first real API call — unlike every other required env var in this codebase, which throws loudly and immediately if misconfigured. Now validated the same way.
- **Real bug, fixed**: `PLAID_WEBHOOK_URL` was read directly with no presence check, unlike every other required secret in the codebase (`PLAID_CLIENT_ID`/`SECRET`, `SUPABASE_URL`/`SERVICE_ROLE_KEY`, `TOKEN_ENCRYPTION_KEY` — all throw loudly at module load if missing). If unset, Plaid created Items with no webhook configured at all — a silent, permanent "never receives `SYNC_UPDATES_AVAILABLE`, relies on manual pull-to-refresh forever" failure mode with no log or error anywhere pointing at the cause. Now throws the same way as the others.
- **Real, low-severity bug, fixed**: `previousMonthTotal` in `/api/transactions/monthly` summed Postgres `numeric` values (serialized as bare JS numbers by PostgREST) via repeated floating-point addition with no rounding — could produce results like `1200.9999999999998` instead of the exact `1201.00` for a large enough set of rows. Now rounded to cents, since the error here is always sub-cent, unlike the general case of rounding an arbitrary float.
- **Minor input-validation gaps, fixed**: `exchange-token.ts`'s `publicToken` and `confirm.ts`'s `transactionId`/`calendarEventId` were checked for truthiness only, not type — a wrong-typed value (e.g. an object) would reach Plaid's SDK or a Postgres `uuid`-column comparison and surface as a generic 500 rather than a clean 400. Low real-world impact (these endpoints are only reachable by an authenticated user's own trusted mobile client), fixed anyway since it was cheap and correct.
- **Real bug, fixed and independently verified against the installed SDK's types**: `plaidWebhookVerify.ts` cached a webhook verification key by `kid` but never checked its `expired_at` field — confirmed this field is real and part of Plaid's `JWKPublicKey` type (`node_modules/plaid/dist/api.d.ts`), and part of Plaid's documented, required verification algorithm: a retired key (e.g. after a suspected compromise) can still validate a signature cryptographically even though Plaid has explicitly marked it no longer trustworthy. Fixed: the cache now stores `expired_at` alongside the key and `verifyPlaidWebhook` rejects outright if it's non-null. Since a `kid` that's expired stays expired permanently (never un-retires under the same id), this is also cached correctly rather than re-checked on every call.
- **Follow-up real bug in that same fix, found on a fresh read-through and fixed**: the cache itself never expired an entry once fetched — meaning that if a kid was cached while still valid (`expiredAt: null`) and Plaid retired it *later* (the exact "suspected compromise" scenario the `expired_at` check above exists to catch), a warm serverless instance would keep accepting signatures from that now-retired key indefinitely, since nothing ever re-fetched an already-cached entry. Checked Plaid's own webhook verification docs directly for a recommended cache TTL before picking one — they don't give one, and their own reference sample code across every language caches the same unbounded way this file originally did, so this is a real, industry-common gap, not something already covered by following Plaid's guidance. Fixed with a 1-hour TTL (`isCacheEntryStale()`, pure and unit-tested for the exact boundary) — bounds how long a warm instance can keep trusting a key Plaid has since retired, at the cost of one extra Plaid API call per kid per hour of continuous webhook traffic. **Caught a real process mistake before letting it stand, not after**: the first live-verification attempt fired a genuine Plaid-signed sandbox webhook and got a clean `200`/no-error log line — but that was *before* this fix had actually been deployed, so it only proved the *old* code still worked, nothing about the new TTL logic. Caught by checking deployment timing rather than assuming the test meant what it looked like it meant; redeployed, fired a fresh webhook, and confirmed via `vercel inspect` that the log's `deploymentId` genuinely matched the just-deployed one before trusting the result: `responseStatusCode: 200`, empty `logs` array, confirming real signature verification still works with the new caching logic actually live.
- **Severe real bug, fixed and verified — recommend rotating `PLAID_SECRET`**: every `console.error(label, err)` call that could log a Plaid API error was logging the *raw* axios error object. Plaid's Node SDK is axios-based, and an axios error's `.config`/`.request` embed the entire outgoing request — for every single Plaid call in this app, that includes the `PLAID-SECRET` header (the one master credential for the whole Plaid integration, not scoped to a user or item) in plaintext, and for item-scoped calls (`itemRemove`, `itemGet`, `transactionsSync`) the decrypted `access_token` too. Proved this for real, not by reading axios docs: forced a genuine Plaid API error with a canary access token and a real request, logged the raw error object, and both the real `PLAID_SECRET` and the canary token appeared in the output in plaintext. This means any Plaid API failure ever hit in production — a bad access token, a transient outage, a malformed request — would have written the master secret straight into Vercel's log retention, undermining the whole point of encrypting tokens at rest. Fixed: `backend/lib/logging.ts`'s `safeErrorInfo()` extracts only `message`, `httpStatus`, and Plaid's own error response body (`error_code`/`error_type`/`error_message` — describes the failure, never echoes back what we sent) and is now used at every log site that touches a Plaid call (and, for defense-in-depth, everywhere else too). Verified against the actual shipped module with the same forced-error technique: no secret, no token, still a useful `error_code`. **Not verifiable from here whether this was ever actually triggered in real production traffic** (would require checking Vercel's log history, which I don't have access to) — rotating `PLAID_SECRET` in the Plaid dashboard is a cheap, high-value precaution given the exposure window existed since early in this project.
- **Follow-up gap in that same function's "defense-in-depth" claim, found on a fresh read-through, fixed**: `safeErrorInfo` only ever special-cased axios-shaped errors and plain `instanceof Error` objects — a genuine Supabase/PostgrestError (by far the single most common error shape in this codebase, since every DB call produces one on failure) is a plain object, not an `instanceof Error` — confirmed directly against a real query failure, not assumed (`error.constructor.name` is `"Object"`). That means every one of this codebase's many `safeErrorInfo(dbError)` call sites was silently falling through to the untyped final `return err`, providing zero actual normalization for its most common input despite the function's own comment claiming it's "used everywhere, for defense-in-depth." An existing test (`logging.test.ts`) had actually locked in this exact gap as expected behavior — updated it along with the fix rather than leaving stale coverage. Not an active secret leak in this codebase specifically (no raw token/secret is ever bound into a query whose failure could echo it back — only `access_token_encrypted`, itself ciphertext, ever reaches the DB), but a real gap in what the function actually protects, closed before it could matter as the schema evolves. Fixed by explicitly extracting `message`/`code`/`details`/`hint` for a detected Postgrest-shaped error, same posture as the Plaid branch above.
- **Real bug, fixed and verified live in production**: `/api/plaid/status` computed `linked` from `plaid_items` rows with `status = "active"` only. A bank needing re-authentication (Plaid `ITEM_LOGIN_REQUIRED` — a normal, expected occurrence, not an edge case) sets that item's status to `"login_required"` via the webhook (`webhook.ts`). For a user with only one linked bank, this silently flipped `linked` back to `false` — the app took that as "never linked" and routed them back through first-link onboarding, and linking again there would create a *second* Plaid item for the same bank rather than fixing the broken one, risking duplicate `synced_transactions` (dedup is per-item) once both were pulling the same account. Fixed: `linked` now means "has completed onboarding at least once" (any `plaid_items` row, any status); a new `needsReauth` field is `true` when any item isn't `active`. Mobile (`App.tsx`) shows a banner directing the user to Settings when this fires — no dedicated Plaid "update mode" reconnect flow exists yet, but relinking via Settings' existing "Add another bank account" is safe (the broken item is excluded from all future syncs by `sync.ts`'s `status = "active"` filter, so it just goes inert rather than duplicating). Verified live: forced a real test item into `login_required` via direct DB write, confirmed the production endpoint returned `{"linked":true,"needsReauth":true}` (the exact broken-before state), confirmed `/api/plaid/sync` still returned 200 with that item excluded rather than erroring, then restored the item and confirmed a clean `{"linked":true,"needsReauth":false}`.
- **Real bug, fixed**: `/api/plaid/sync` looped over a user's `plaid_items` with a bare `await syncPlaidItem(item, filters)` per item, no per-item error handling — one item throwing (a transient Plaid rate limit or network blip, not even an item-level error) failed the *entire* request with a 500, including every other healthy linked account's transactions, even though their data had often already been fetched and stored by that point. Directly undermines the multi-account feature this project added. Fixed: each item's sync is now wrapped in its own try/catch, logged and skipped on failure (that item's cursor simply isn't advanced, so it's retried next sync) rather than failing the whole request.
- **Real bug, fixed**: `exchange-token.ts` pulls an eager first batch of transactions right after creating the `plaid_items` row, so the user doesn't have to wait for Plaid's first webhook. That eager sync call wasn't wrapped in its own error handling — a transient failure there (not a linking failure at all) reported "couldn't link account" to the user even though the `plaid_items` row had already been inserted successfully, i.e. the account genuinely was linked. Worse, the user was then free to retry Link from scratch, which would create a *second* Plaid item for the same bank. Fixed: the eager sync is now best-effort (caught, logged, non-fatal) — the row insert is what actually constitutes "linked"; the regular sync path picks up the item regardless.
- **Real bug, worse than the eager-sync one above, fixed**: found on a fresh read-through of every backend endpoint. `exchange-token.ts` called `itemPublicTokenExchange` (which consumes the single-use `public_token` and returns a live, real `access_token`) and then made two more Plaid calls — `itemGet`, `institutionsGetById` — purely for cosmetic institution name/id enrichment, **before** the `access_token` was ever encrypted and stored anywhere. A transient failure in either enrichment call (the same class of Plaid hiccup already handled as non-fatal for the eager sync) meant the freshly-exchanged `access_token` was never persisted — not just "sync doesn't start yet" like the eager-sync bug, but the credential itself is gone: never encrypted, never stored, and therefore never revocable either, since revoking requires calling `itemRemove` with a token we'd have no record of. The user just saw "couldn't link account" and was free to retry, leaving a real, live, permanently-orphaned credential to their actual bank account sitting at Plaid with nothing in our system aware it exists. Fixed by reordering: the `plaid_items` row is now inserted immediately once the `access_token` exists (institution fields null), and `itemGet`/`institutionsGetById` run afterward as a genuinely best-effort enrichment step with their own try/catch, updating the row if they succeed. Checked whether this enrichment delay has any current user-visible cost before treating it as acceptable, rather than assuming: grepped for `institution_name`/`institutionName` across the whole mobile app — it's stored but never read or displayed anywhere today, so a failed enrichment attempt has zero current UI impact, only a null column that could have been populated.
- Not fixed, flagged rather than silently left: no full Plaid "update mode" re-link flow exists for a `login_required` item — see the `needsReauth` entry above for the current (safe, if imperfect) workaround.
- **Checked and confirmed clean, not assumed**: `0001_init.sql`'s RLS policies grant `authenticated` SELECT on `users`/`plaid_items`/`synced_transactions` scoped to `auth.uid()` — on paper, that means a user's own session JWT plus the app's embedded publishable key could read their own row directly via Supabase's REST API, including `plaid_items.access_token_encrypted` (ciphertext, but unnecessary exposure since the mobile app never calls `supabase.from(...)` at all — confirmed by grep, it only ever uses `supabase.auth.*`). In practice this is fully blocked one layer lower: `0002_grants.sql` only ever granted table privileges to `service_role`, never to `authenticated`/`anon`, so Postgres denies the query before RLS is even evaluated. Verified live against production: direct REST reads of all three tables with a real user's own valid access token return `403 permission denied for table ...`, while the backend's service-role key still reads fine. Direct database access via the embedded key is a dead end today — but this makes the RLS policies themselves unreachable dead code resting on an easy-to-break assumption (one future `GRANT SELECT ... TO authenticated` — e.g. added for some unrelated feature — would silently reactivate them exactly as originally written, including the `access_token_encrypted` exposure). Worth tightening properly at some point (drop the SELECT policies entirely, or exclude `access_token_encrypted` via a view) rather than relying on the current grants never changing; not urgent since nothing in the deploy process would add such a grant by accident.

### Database
- Postgres via Supabase (also provides auth, so we get user accounts for free)
- Schema defined in `backend/supabase/migrations/0001_init.sql`: `users`, `plaid_items` (encrypted access_token, item_id, institution, sync cursor), `synced_transactions` (dedupe key, calendar_event_id, status)
- Backend writes with the service-role key (bypasses RLS entirely — the backend's own per-request user-scoping is the real protection there, not RLS). RLS policies exist on all three tables but are currently unreachable dead code — see the Backend resilience finding below for why, and the pending migration (`0004_drop_unused_select_policies.sql`, not yet confirmed applied) that closes the gap for good rather than relying on that staying true.
- `0005_add_last_synced_at.sql` adds `plaid_items.last_synced_at`, backing the sync rate-limiting fix (see Backend resilience) — also not yet applied. `sync.ts` detects the column's absence and runs correctly either way (see that entry), so there's no strict ordering requirement between applying this and deploying the code, but the cooldown protection only actually takes effect once it's run.
- `0006_nullable_revoked_access_token.sql` drops the `NOT NULL` constraint on `plaid_items.access_token_encrypted`, backing the fix that nulls it out on `USER_PERMISSION_REVOKED`/`USER_ACCOUNT_REVOKED` (see Backend resilience) — also not yet applied. `webhook.ts` probed the real failure mode against production before shipping (Postgres's `23502` not-null-violation) and falls back to a status-only update on exactly that code, so this is also safe to deploy in either order.

### Auth
- Supabase Auth — so a user's Plaid connection persists across app reinstalls / device changes
- Session storage moved from plain `AsyncStorage` to `expo-secure-store` (`mobile/src/lib/supabase.ts`, iOS Keychain / Android Keystore-backed) — runtime-verified on a fresh EAS build (see the runtime-verified entry under Mobile UI/UX below): real sign-up, sign-in, and persisted-session-survives-relaunch all worked against it. One real bug surfaced during that test: `/api/plaid/status` returned 401 a few times right after sign-in, then succeeded on retry with the same session and no code change — not fully root-caused (leading theory: propagation lag from confirming the test account via the Supabase admin API rather than a real email link), so `authedFetch` (`mobile/src/lib/api.ts`) now retries once via a forced session refresh on any 401 before surfacing an auth error.

### Mobile testing
- **Structural gap, closed for the pure-logic half — same posture as the backend's own Vitest suite**: every one of this session's real mobile bugs (the noon-UTC calendar anchor, the Insights `getUTCDate`/`getDate` timezone bug, the USD-default currency bug, the multi-page sync duplicate/miscount bug, the per-user calendar storage key leak) was verified once with reasoning or a one-off script, then never protected against regressing. Added Vitest (`mobile/vitest.config.mts`, `npm test`, 34 tests across 7 files) — deliberately scoped to RN-import-free pure logic only, matching the backend's own "don't fake what a real device run actually proves" posture. Required extracting logic that used to live inline inside RN-importing files (`calendar.ts`, `InsightsScreen.tsx`, `App.tsx`) into new dependency-free modules under `src/lib/`, since Vitest can't import a file that pulls in a native module (`react-native-calendar-events`, `react-native-safe-area-context`, etc.) without it: `dates.ts` (`localNoonIso`, `monthLabel`, `shiftMonth`, `daysInMonth`, `currentMonth`, `daysElapsedInMonth`), `currency.ts` (`formatCurrency` — also de-duplicates what used to be two near-identical copies in `calendar.ts` and `InsightsScreen.tsx`), `percentages.ts` (`roundPercentagesToSum100`), `storageKeys.ts` (`calendarStorageKey`), `syncAccumulator.ts` (`createSyncAccumulator` — the actual fix for the multi-page dedup bug, not just its test). All five original files now import from these instead of duplicating the logic inline — single source of truth, not a parallel copy kept only for testing.
- **Real bug caught while writing the tests themselves, not before**: the first version of `daysElapsedInMonth`'s test embedded a `-07:00` offset directly in an ISO string (`new Date("2026-09-30T23:00:00-07:00")`) and asserted on `.getDate()` — which reports in the *running process's* configured local timezone, not whatever offset the string was constructed with. Failed for real on this exact machine (`America/Toronto`, not Pacific), not hypothetically. Fixed by explicitly pinning `process.env.TZ` before each assertion (confirmed this reassignment actually takes effect at runtime in this Node version before relying on it, not assumed) — the same technique `localNoonIso`'s own tests already used correctly. Without this fix, the suite's result would have depended on which machine or CI runner happened to execute it.
- **Deliberately out of scope for now, same reasoning as the backend's own carve-out**: full component rendering (screens, navigation, Plaid Link, calendar permission flows) and anything touching AsyncStorage/network/native-module mocking — that's real integration behavior this session verified against actual Simulator runs and direct EventKit database queries by design, and turning that into a mocked test suite would prove far less than it looks like it proves. This suite starts with the pure, RN-import-free logic; a real component-test strategy (`@testing-library/react-native` + `jest-expo`, likely, given Vitest's own React Native support is limited) is a deliberate, separate follow-up.

### Mobile UI/UX (findings from a dedicated craft review, prompted by user doubt the app felt "vibe coded")
- **Real, visible accuracy bug, fixed**: every dollar amount defaulted to (`calendar.ts`'s `formatAmount`) or was hardcoded to (`InsightsScreen.tsx`'s `formatCurrency`) `"USD"` — for an app whose one documented market is a Canadian bank. `Intl.NumberFormat` doesn't convert currency, only how it's displayed, so no number was ever actually wrong, but every calendar event title and every figure on the Insights screen (total spend, category breakdown, day-by-day list) showed a plain `$` as if it were US dollars. Confirmed the real, visible difference rather than assuming it was cosmetic: `Intl.NumberFormat("en-US", {currency: "USD"}).format(42.5)` → `"$42.50"`, `{currency: "CAD"}` → `"CA$42.50"`. Fixed both to default to `"CAD"` instead. `InsightsScreen`'s aggregate totals (summed across a whole month, potentially spanning more than one linked account/currency now that multi-account is supported) keep CAD as a best-effort default since a single label there is always an approximation regardless — but its one call site with real per-transaction data (the day-by-day list) now passes the transaction's own `isoCurrencyCode` instead of guessing, since that data was already available and unused.
- **Real bug, fixed — found by tracing what account deletion actually leaves behind on-device**: `App.tsx`'s selected-calendar `AsyncStorage` key was a single global key, never scoped per user and never cleared on sign-out or account deletion. Concretely: delete your account and sign up fresh on the same device, and the brand-new account silently inherited the deleted account's calendar choice — skipping `CalendarPickerScreen`, and the privacy confirmation it shows, entirely. Same mechanism on a shared device with two different accounts: whichever signed in most recently would leave its choice for the other to inherit on next sign-in. Fixed by scoping the key to the signed-in user's id (`selectedCalendar:<user-id>`) rather than clearing-and-hoping — Supabase never reuses a deleted user's id for a new account, so a fresh signup always reads an empty key with no explicit cleanup needed, and two accounts on one device never collide. Verified with a real test: account A's stored choice stays intact but fully isolated; a brand-new account B reads `null` and correctly starts fresh.
- **Real bug found by checking the interaction between two features built this session, not caught when either shipped alone**: `HomeScreen`'s pending-events map (dedupes a retried confirm against a create that already succeeded — see below) only ever cached `eventId`, not which calendar it was created on. If a create-succeeds-confirm-fails transaction sat unconfirmed and the user then used "Change calendar" (a separate feature added this session) before the next retry, the retry would silently reuse the cached `eventId` — an event that still lives on the *old* calendar — so the transaction the user explicitly moved to a new calendar would silently stay on the one they'd just moved away from. Fixed: the cache now stores `{eventId, calendarId}`; a cached entry is only reused if its `calendarId` matches the current selection, otherwise the stale event is best-effort removed from the old calendar (`removeTransactionEvent()`, never throws — the old calendar could itself be gone) and a fresh one is created on the calendar the user actually picked. Verified with a real test covering both paths: same-calendar retry still reuses the cached event (no regression), calendar-switch retry creates fresh on the new calendar and cleans up the orphaned one.
- **Real, severe bug, fixed**: `CalendarPickerScreen` — a required gate in front of the *entire app* for every single user — rendered the exact same "No writable calendars found on this device" empty state whether there genuinely were no calendars or the user had simply tapped "Don't Allow" on the permission prompt (an entirely ordinary thing for a real user to do, not misuse). Denying permission was a permanent dead end: no retry, no path to Settings, nothing recoverable short of quitting the app, finding Settings unprompted, granting access, and relaunching from scratch. Fixed: a distinct `permissionDenied` state shows the real reason, an "Open Settings" button (`Linking.openSettings()`) gets the user there directly, and a "Try again" button re-runs the permission check on return — the standard, expected recovery loop for a denied OS permission.
- **Real bug in that same pagination fix, found on a fresh read-through and confirmed with a simulation before fixing, not assumed**: the backend's "pending page" query (`sync.ts`) has no offset/cursor — it's always "the current top 100 pending rows by date." A transaction that fails mid-sync stays `"pending"` server-side, so a *later* page within the same multi-page loop can re-fetch and re-attempt the exact same transaction. `HomeScreen.handleSync()`'s `synced` accumulator was a plain array, so a transaction re-attempted this way got pushed into it twice — simulating 150 pending transactions with 10 failures on page 1 produced 160 pushes for only 150 distinct ids, confirmed with a real script before touching any code. Two concrete, visible consequences: `FlatList`'s `keyExtractor={(txn) => txn.id}` sees duplicate keys (undefined React reconciliation, not just a console warning), and `synced.length` — used for both the live progress counter and the final "X of Y couldn't sync" summary — overcounts real attempts (e.g. "Syncing 160 of 150…", a visibly broken progress bar). Fixed by keying `synced` as a `Map<string, SyncedTransaction>` instead of an array, so a later re-attempt's outcome simply overwrites the earlier one for that transaction. This turned out to fix more than deduplication: re-running the same simulation against the fix showed the 10 that failed on their first attempt correctly ended up reported as synced, since they succeeded on the same-sync retry — the array version would have permanently misreported them as failed even though the operation actually succeeded by the time it finished.
- **Real bug, fixed and verified with a real timing test (including the failure mode my own fix could have introduced)**: `/api/plaid/sync` returned *every* pending transaction in one response with no limit. Under normal use this stays small, but a user who doesn't open the app for weeks — an ordinary pattern for a passive spending diary, not misuse — can accumulate a large backlog purely from Plaid's webhook-driven syncing running the whole time in the background. One huge response meant `HomeScreen`'s sequential per-transaction loop (each a real device-write-plus-network round trip) could run for a very long time before showing anything finished. Fixed with backend pagination (`hasMore`/`total`, 100 per page) and a mobile-side loop that keeps calling sync until exhausted, showing accurate progress across the whole operation rather than resetting per page. Caught a real bug in that fix before shipping it, not after: a page where *every* transaction fails (e.g. calendar access revoked mid-sync) would never actually shrink the server-side pending count, so re-fetching the "next" page would return the identical stuck page forever. Added a guard — bail once a full page confirms zero successes — and proved both behaviors with a real test: 250 pending transactions across a 100-item page size correctly took exactly 3 iterations and processed all 250; a fully-stuck page correctly broke out after exactly 1 iteration instead of looping, with everything in it honestly marked "failed" (remaining "pending" server-side for the next real attempt, not lost).
- **Real bug, fixed and independently verified as plausibly explaining an earlier unresolved mystery**: `App.tsx`'s `onAuthStateChange` callback discarded the event name entirely (`_event`) and re-armed `booting` (which unmounts/remounts the entire app behind a bare spinner) for *any* truthy session — not just a genuine sign-in. Confirmed against `@supabase/auth-js`'s actual `AuthChangeEvent` type: it also fires `TOKEN_REFRESHED` (routine, roughly hourly via `autoRefreshToken: true`), `USER_UPDATED`, and `INITIAL_SESSION`. Fixed to only re-arm on `event === "SIGNED_IN"`. This is a plausible real explanation for the transient "401 right after sign-in, fixed by retrying" symptom hit during live testing earlier — a stray re-mount racing the in-flight status check — rather than just a cosmetic flash; the retry-on-401 fix in `authedFetch` stays regardless since it's still real, correct resilience for whatever else could cause a stale token.
- **Real bug, fixed and proven with a before/after test**: `createTransactionEvent()` anchored a date-only transaction (no `datetime` from Plaid) to `${date}T12:00:00.000Z` — noon **UTC**, an absolute instant. EventKit displays a non-recurring event by converting its absolute instant to the *viewing* device's current timezone (setting `CalendarEventWritable.timeZone` doesn't change this for a single event — confirmed by reading the library's own type docs — that field mainly affects recurrence math). A user materially west of UTC saw a "noon" transaction rendered as their own early morning. Verified the exact before/after with `TZ` env var simulation: old code showed 5:00 AM in Vancouver / 9:00 PM in Tokyo for the same transaction; new code (`localNoonIso()`, building the `Date` from local components so `.toISOString()`'s local→UTC conversion round-trips correctly) shows 12:00 PM in both. Also now sets `timeZone` on the written event for correctness, though the real fix is the anchor construction, not that field.
- **Same class of timezone bug, found separately in a different file, fixed**: `InsightsScreen`'s "average per day" figure computed how many days to average the current month over via `new Date().getUTCDate()`, while the same file's own `currentMonth()` (right above it) determines "is this the current month" from *local* date components (`getFullYear()`/`getMonth()`) — the two disagree for a real chunk of every evening in any timezone behind UTC, which is this app's *entire* real market (every Canadian timezone is west of UTC). Verified for real, not assumed: simulating 11:30 PM in Vancouver on the 14th, `getUTCDate()` already reports 15 (UTC has rolled to the next day) while `getDate()` correctly still reports 14. The inflated day count understated `avgPerDay` for that whole window. Fixed to `getDate()`, matching `currentMonth()`'s own local basis.
- **Real bugs found and fixed — reentrancy guards missing beyond the one already added to `HomeScreen`**: `LinkAccountScreen`'s "Connect a bank account" only guarded with `disabled={loading}` (async React state) — the same race class already fixed elsewhere, but here a double-tap landing before re-render could start two concurrent Plaid Link sessions and, if both completed, link the *same bank* as two separate Plaid Items (two distinct public tokens exchanged), silently double-diarying every one of that bank's transactions going forward. Added the same `useRef` guard pattern as `HomeScreen`.
- **Same gap, found later in `AuthScreen`, fixed for consistency**: `handleSignIn`/`handleSignUp` had the identical `disabled={loading}`-only race. Lower real-world severity than the two above (no duplicate bank Item or calendar event results either way), but a double-tapped "Sign up" can fire two concurrent `signUp()` calls for the same email, one succeeding and one surfacing a confusing "already registered" error racing the real "Check your email" alert — a real, if minor, user-facing glitch rather than a theoretical one. Same `useRef` guard pattern applied.
- **Real bug found and fixed**: `InsightsScreen`'s month-fetching had a genuine race, and it went deeper than the existing `cancelled` flag actually protected — that flag only ever guarded `setError`/`setLoading`, never the `setTransactions`/`setPreviousMonthTotal` calls inside `fetchMonth` itself, which ran unconditionally. Changing months quickly (re-firing the effect while an older fetch is still in flight) or pulling to refresh independently of the effect could let a stale, slower response overwrite a newer, faster one's data. Fixed with a single incrementing request-id counter, passed explicitly into each `fetchMonth` call rather than predicted or returned (a design where `fetchMonth` returns its own id was tried and rejected: a rejected promise never runs a `.then`, so the id would be unknown in `.catch` exactly when the staleness check matters most). Verified with a real timing test: an older, slower request for a previous month resolving *after* a newer, faster request for the current month correctly did not overwrite it.
- **Real bug, fixed**: `SettingsScreen`'s `Number(minAmountText)` only understood a period decimal separator, but `keyboardType="decimal-pad"` shows the OS's locale-appropriate separator — a comma on a French-Canadian device (relevant given the bank is Canadian) — so a normal amount typed on the exact keyboard the screen itself presents failed as "Invalid amount". Fixed by normalizing a comma to a period before parsing.
- **Real bug, fixed (narrower in practice than first suspected — checked against the actual SDK rather than assumed)**: `confirmDeleteAccount` wrapped `deleteAccount()` and `supabase.auth.signOut()` in one try/catch, so a failure in the sign-out step after a successful deletion could report "Couldn't delete account" — a false negative on something that had already succeeded. Read `@supabase/auth-js`'s actual `_signOut` implementation: it already treats a 404/401/403 (exactly what signing out a just-deleted user produces) as expected and returns `{error: null}` without throwing, so the practical trigger is narrower than a normal network/auth hiccup — only a genuine unhandled exception (e.g. a SecureStore failure clearing the local session) would reach it. Still fixed with separate try/catch blocks, since the false-negative message would be wrong regardless of how rare the trigger is.
- **Real confusion during the user's own live testing, traced to source, fixed with a copy change**: the user excluded `TRANSFER_IN`, saved, synced again, and the numbers didn't change — looked like the filter wasn't working. Traced it with real evidence rather than assuming either way: queried the actual DB and found the 3 `TRANSFER_IN` transactions all had `status: "synced"` with the identical `created_at` timestamp, from the eager sync `exchange-token.ts` already runs automatically right after linking — before the user could ever reach Settings to set a filter. This is correct, designed behavior (a filter can't retroactively un-sync something already written to the calendar, matching this app's own established stance elsewhere that it never auto-deletes calendar events), but `SettingsScreen`'s copy for both filters ("Charges below this amount won't be synced", "Transactions in these categories are never synced") didn't say anything about being forward-only, which is exactly why this read as a bug. Fixed by making both explicitly say "From now on..." and "...aren't affected" for transactions already on the calendar.
- **Real UX feedback from the user during this session's own live testing, fixed**: `SettingsScreen`'s excluded-category rows used a custom tap-to-toggle circle-with-checkmark control. The user, testing the app live on the fresh build, flagged this directly — wanted a real iOS-style toggle switch instead. Swapped to React Native's native `Switch` (with the app's `seriesBlue` as the on-track color, matching the rest of the UI's accent), keeping the whole row tappable via the existing `Pressable` (a bigger, easier target than the switch control alone) with the `Switch` itself set `pointerEvents="none"` so the row's own tap handler is the only thing toggling it, not both at once. `accessibilityRole` updated from `"checkbox"` to `"switch"` to match.
- **Real bug caught live during the same testing session, not from code review**: a Metro log line surfaced during a routine reload — `App Start Span could not be finished. Sentry.wrap was called before Sentry.init` — appearing on every single app launch. Root cause: `index.ts` called `Sentry.wrap(App)` unconditionally, but `initSentry()` no-ops (never calls `Sentry.init`) whenever the DSN is empty, which it currently is. `Sentry.wrap` itself degrades safely without an initialized client (same posture as `captureError`), so this was never a functional bug — but it meant every launch produced this confusing warning for no reason. Fixed by having `initSentry()` return whether it actually initialized, and only calling `Sentry.wrap` when it did (`App` is registered directly otherwise). Verified live, not assumed: the warning is gone from a real Metro log after the fix, confirmed on the same running Simulator session where it was first found.
- **Accessibility gaps beyond what was previously marked "all fixed"**: that claim wasn't accurate app-wide — `AuthScreen` (Sign in/Sign up/Privacy Policy), `LinkAccountScreen` (Connect/Cancel), `SettingsScreen` (Save/Add account/Change calendar/Delete account — a destructive, irreversible action that had zero accessibility signal), and the `App.tsx` tab bar all had interactive elements with no `accessibilityRole`/`Label`, sitting in the same files as other elements that already had them (an incomplete pass, not a deliberate omission). Also fixed the tab bar's active-tab indicator, which was color-only (`textMuted` vs `textPrimary`, nothing else) — added `fontWeight` as a second signal and `accessibilityState={{selected}}` for screen readers. All addressed.
- **Real bug, fixed and proven with a before/after test**: `HomeScreen.handleSync()`'s per-transaction loop called `createTransactionEvent()` (a local device write) then `confirmCalendarEvent()` (a network call) inside one try/catch. If the calendar write succeeded but the confirm call failed — a network drop or backend hiccup between the two, entirely plausible on a real phone — the transaction stayed `"pending"` server-side forever (only a successful confirm flips it to `"synced"`), so the *next* sync fetched the same transaction again and created a **second** real calendar event for it. Directly contradicts this project's own "one calendar event per transaction" constraint. Proved both the bug and the fix with a standalone logic test (mocking just the AsyncStorage/network calls, not the RN runtime): the old loop created a second `event-2` on retry with `event-1` left orphaned on the device; the new loop persists `{transactionId: eventId}` to `AsyncStorage` (`mobile/src/screens/HomeScreen.tsx`'s `PENDING_EVENTS_KEY`) right after a successful create and *before* attempting confirm, so a retry (later in the same sync, or an entirely new one) reuses the existing event and only retries the confirm call — createCallCount stayed at 1 across two passes where confirm failed then succeeded, verified explicitly, not assumed.
- **Real bug, fixed**: `AuthScreen`'s `handleSignIn`/`handleSignUp` had zero client-side validation — caught live during actual end-to-end testing, not hypothetically: submitting a blank email calls Supabase with an empty string, which it treats as an anonymous-sign-in attempt and rejects with `"Anonymous sign-ins are disabled"`, a real but meaningless message to someone who just forgot to type an email. Fixed with a trivial local check before either call.
- **Fixed**: raw stringified JS errors shown directly in native alerts in 7 places (`String(err)` producing things like `Error: /api/plaid/sync failed (500): {"error":"..."}`), zero `KeyboardAvoidingView` anywhere (text inputs in `AuthScreen`/`SettingsScreen` could get covered by the keyboard), zero accessibility labels anywhere (icon-only controls like Insights' `‹`/`›` month arrows had nothing for a screen reader), zero pull-to-refresh despite two list/scroll screens, and `HomeScreen`'s sync loop firing a "Success" haptic even when some/all transactions failed to write with no progress feedback during a real multi-second per-transaction operation. All fixed — see the commit for the full list; this isn't exhaustive polish, just what a direct read-through of every screen actually turned up.
- **Placeholder improved on a second pass — still not a finished, professionally-designed asset**: `mobile/assets/icon.png`/`splash-icon.png` were originally Expo's unmodified default template images, then a first placeholder (a calendar card + generic checkmark). Redesigned again: a calendar card with a bold `$` where a date number would normally sit — a more distinctive, on-brand visual pun ("calendar + spending," not "generic todo/task app," which a checkmark could belong to) — still built with Pillow at 1024x1024 using the app's own existing theme colors (`seriesBlue`, `statusGood`, plus a darker header shade in the same blue family), not invented ones. Checked legibility at real icon sizes before finalizing, not just at full resolution: resized to 180/120/60px (actual iOS home-screen sizes) and confirmed the calendar shape and `$` both stay clearly readable even at 60×60. Verified via a real `expo prebuild` that it lands correctly in the generated `AppIcon.appiconset`. Still worth a real designer pass before App Store submission — this is a meaningfully more distinctive placeholder, not a finished asset.
- **Fixed, real bugs not just polish**: `App.tsx`'s root bootstrap logic had a genuine flash-of-wrong-content bug, not a cosmetic one — `linked` defaults to `false` and `booting` only ever gated the session check, so every already-linked user briefly saw "Connect your bank" on cold launch before the async Plaid-status check resolved and flipped it. Same class of bug could recur mid-session too (sign-out/sign-in, including the one `Settings → Delete account` itself triggers). Also, a failed status check was silently treated as "not linked" rather than shown as an error — a network hiccup could route an already-linked user back through the first-link flow. All fixed: `booting` now covers the full session+status determination window (re-armed on any fresh sign-in event, not just at mount), and a real status-check failure shows an explicit error with a "Try again" button instead of guessing. Also added a missing try/catch around the calendar `AsyncStorage` read (corrupted local data would have thrown inside an unhandled `.then()`).
- **A regression of that exact same bug class, found in the "Try again" retry path this fix itself added, fixed**: the status-check effect clears `statusError` synchronously the moment it re-runs, well before the retried `getPlaidStatus()` call actually resolves — but the "Try again" button never re-armed `booting`, which the first failed attempt's own `finally()` had already set back to `false`. Traced the render sequence rather than assuming: for however long the retry takes, that left `statusError=null`, `booting=false`, and `linked` still at its default `false` (never having succeeded) — falling straight through to `LinkAccountScreen`, i.e. an already-linked user briefly told to connect their bank again, exactly the bug this whole fix exists to prevent, just reintroduced via the retry path. Fixed by setting `booting(true)` directly in the button's `onPress`, not inside the effect itself — the effect also re-runs on every routine hourly token refresh (since `session` gets a new object reference then too), and setting `booting` there unconditionally would flash the full boot spinner on every refresh, a worse regression than the one being fixed.
- **Real bug, fixed**: there was no way to change the selected calendar once chosen — not even signing out and back in, since that only reset `App.tsx`'s in-memory `calendar` state, not the persisted `AsyncStorage` value the next sign-in would just reload unchanged. A mis-tap during onboarding, wanting to switch calendars, or the chosen calendar being deleted from the device were all unrecoverable short of reinstalling the app. Fixed: `handleChangeCalendar()` clears the stored value and resets state (naturally re-routes to `CalendarPickerScreen`, since that's already gated on `!calendar`); exposed as a "Change calendar" button in Settings with a confirmation dialog, next to "Add another bank account".
- **Real bug in my own fix, caught and closed**: the retry-on-401 logic added to `authedFetch()` (see the Auth section's session-storage entry) called `supabase.auth.refreshSession()` unguarded — its own docs say it throws (rather than returning `{error}`) for some failure modes, e.g. a network error during the refresh call itself. That would have thrown out of `authedFetch` with a raw, inconsistent error instead of falling through to the existing clean 401-message handling. Wrapped in try/catch; a failed refresh now just falls through to the original response as intended.
- **Real bug found and fixed, plus a deeper accessibility gap found and consciously accepted**: `theme.ts`'s `CATEGORY_MAP` assigned `FOOD_AND_DRINK` the exact same hex (`#3987e5`) as `theme.seriesBlue` — the color reserved for the Insights bar chart — so a user with Food & Drink spending saw its legend swatch and every daily bar in identical color on the same screen, contradicting the file's own design comment. Fixed by using the dataviz skill's validated slot 8 (red) instead of guessing a replacement color. While fixing it, ran the actual validator (`--pairs all`, since this screen sorts categories by spend amount — a data-dependent order where any two of the 7 can end up adjacent) and found the palette's own reference doc explicitly limits all-pairs CVD-safety to its first 3 slots, folding anything past that into "Other" — this app's use of 7 simultaneous category colors was never fully colorblind-safe, confirmed by a real FAIL between magenta/aqua that pre-dates this fix entirely (independent of the blue-collision bug). Asked the user how to handle it: ship as-is, since every legend row already pairs its color with a text label (color is never the sole signal) — not reducing to ~3 colors + "Other" today. Documented as a known, deliberately-accepted limitation in `theme.ts`, not a silent gap.

### Observability
- **Real gap in the previous Sentry setup itself, found on a fresh read-through and closed**: `initSentry()` only ever wires up Sentry's automatic global handlers — uncaught exceptions and render crashes. Every one of this app's ~9 `try/catch` blocks that catches an error and handles it locally (an Alert to the user, a state update) was invisible to Sentry entirely, since a caught exception never reaches those global handlers — this is the exact gap the original Sentry work's own reasoning ("nothing on-device reports anywhere if something goes wrong") was meant to close, just not actually closed for the most common failure shape in the app. The worst instance: `HomeScreen`'s per-transaction sync loop had *zero* signal anywhere — not even a `console.error` — for a single transaction failing to write to the calendar, the single most operationally important failure mode in the entire app. Fixed with `captureError()` (`mobile/src/lib/sentry.ts`), called at every real catch site across `App.tsx`, `HomeScreen`, `LinkAccountScreen`, `SettingsScreen`, `InsightsScreen`, and `AuthScreen`'s Turnstile handler (skipped `AuthScreen`'s actual sign-in/sign-up catches — those only ever catch an already-self-reporting `getCaptchaToken()` failure, Supabase's own wrong-password/etc. cases return `{error}` rather than throwing, so there was nothing further to report there). Confirmed safe to call unconditionally, including before `initSentry()` has ever called `Sentry.init` (an empty DSN skips that entirely): read the installed SDK's own `Scope.captureException` — it checks for a configured client and simply returns without throwing if there isn't one.
- **Real gap, being closed**: unlike the backend (which has real, if basic, visibility via Vercel's own log retention — that's how the `PLAID_SECRET`-leak bug earlier this session was actually found), the mobile app had zero crash/error reporting. If something broke for a real user on their own phone, there was no way to find out short of them explicitly saying so. Added `@sentry/react-native` (`~7.11.0`, matched to Expo SDK 57 via `expo install`), initialized in `index.ts` before anything else (`initSentry()`) plus wrapped with `Sentry.wrap(App)` for a React error boundary — together these catch both uncaught async exceptions and render-time crashes automatically, no other code changes needed. `mobile/src/lib/sentry.ts` reads the DSN from `app.config.ts`'s `extra.sentryDsn` (a DSN is a *publishable* identifier by Sentry's own design — safe to embed the same way as the Plaid/Supabase values already there, it only lets a client send events, not read/manage the project) and no-ops safely on an empty one, so this is safe to ship before a real Sentry project exists. Performance tracing (`tracesSampleRate`) is deliberately `0` — a separate, quota-consuming thing from error reporting, not turned on as a side effect of the actual ask. **Not yet complete**: the DSN itself is still an empty placeholder — the user is creating a free Sentry account; once they provide the real DSN it goes into `app.config.ts`, and since this is a new native dependency, needs a fresh EAS build to actually take effect (same pattern as `expo-secure-store` earlier this session — a JS-only Metro reload isn't enough for a new native module). `expo-doctor` run after installing: no new issue introduced (the one pre-existing New-Architecture warning is unrelated and already verified live).

### Mobile security (findings from a dedicated review)
- **Real, documented gap being closed — Cloudflare Turnstile brute-force protection**: checked Supabase's own docs and community-reported pentest findings directly rather than assuming Supabase's defaults were sufficient — Supabase Auth's built-in rate limiting is IP-based only (a token-bucket allowing bursts up to ~30 requests), and its CAPTCHA-based "Attack Protection" is **off by default**; confirmed this project has it off too, since `AuthScreen.tsx` never sent a `captchaToken` before this change. Built the mobile-side half: `mobile/src/components/TurnstileChallenge.tsx` (a `react-native-webview`-hosted Cloudflare Turnstile widget — there's no native RN SDK, this is the standard approach) exposes an imperative `execute()` returning a token, wired into both `signInWithPassword`/`signUp` via `options.captchaToken`. Confirmed by reading `@supabase/auth-js`'s own source that passing `captchaToken: undefined` (the not-yet-configured state) serializes identically to omitting it — safe to ship before Turnstile exists. Reads the site key from `app.config.ts`'s `extra.turnstileSiteKey` (empty placeholder currently) the same way `sentryDsn` works — a Turnstile *site* key is meant to be public by Cloudflare's own design, unlike the *secret* key, which goes directly into Supabase's dashboard and never touches this codebase. **Not yet complete**: needs the user to create a free Cloudflare Turnstile widget (site key comes here), and separately toggle Supabase's dashboard Attack Protection setting with the corresponding secret key (their side, not something I can do) — then needs a fresh EAS build since `react-native-webview` is a new native dependency.
- **Real dead-end bug in that same component, found on a fresh read-through and fixed before it could ever be hit**: `execute()`'s returned promise only ever resolved or rejected from `handleMessage`, which only fires once the injected page's own `window.turnstile.render()` actually runs. If Cloudflare's script never loads at all (no network, the CDN unreachable, a WebView content-blocking policy) the page's own `waitForTurnstile()` polling loop just retries forever — `render()` is never called, so neither the success nor error callback path ever fires, and the promise never settles either way. Since `getCaptchaToken()` is `await`ed directly before `handleSignIn`/`handleSignUp` ever reach Supabase, this would hang sign-in/sign-up indefinitely with the loading spinner stuck and zero error shown — the same class of dead end as the already-fixed calendar-permission one. Not reachable today (Turnstile is still inert, empty site key), but fixed proactively rather than left for whoever hits it first once it's turned on: added a 15s timeout that rejects with a clear, actionable message if `handleMessage` hasn't already settled the promise.
- **App Store blocker — fixed and verified end-to-end, not assumed**: `expo-dev-client`'s launcher screen used to ship in *every* EAS build profile, including `production` (the `eas.json` `developmentClient` flag has no effect on this). Traced through `expo-dev-launcher`'s `EXDevLauncherController.m` and confirmed empirically: excluding `"expo-dev-client"` from `app.config.ts`'s `plugins` array alone is **not sufficient** — it left the generated `AppDelegate.swift` byte-identical, because CocoaPods autolinking (`use_expo_modules!` in the generated Podfile) links every Expo native module found in `node_modules` regardless of the `plugins` array, and that's what actually wires `expo-dev-launcher`'s AppDelegate-subscriber hook into the autolinking-generated `ExpoModulesProvider.swift`. The real fix is `mobile/plugins/withExcludeDevClientFromProductionAutolinking.js`, applied only when `EAS_BUILD_PROFILE === "production"` (`mobile/app.config.ts`, which replaced the old static `app.json`): it passes `exclude: ['expo-dev-client', 'expo-dev-launcher', 'expo-dev-menu']` to `use_expo_modules!`. Verified by running real `expo prebuild` both ways: production profile → zero `DevLauncher`/`DevMenu` references in `ExpoModulesProvider.swift` and `Podfile.lock`; no profile set (normal local dev) → both present as before. `expo-updates` deliberately stays in all profiles including production (legitimate for real OTA updates) — only the interactive dev-launcher UI is excluded.
- **Calendar privacy — mitigated, not eliminated**: `createTransactionEvent()` (`mobile/src/lib/calendar.ts`) still puts the merchant name and exact dollar amount directly into the calendar **event title** — that's the app's core feature, not something to remove. What's fixed: `CalendarPickerScreen.tsx` now confirms with the user before writing to a calendar, naming the actual calendar and its source and spelling out exactly what becomes visible. Checked `react-native-calendar-events`'s `Calendar` type first — no field indicates whether a calendar is actually shared (title/source/isPrimary/allowsModifications only), so this can't be auto-detected and gated only for the risky case; the confirmation runs every time instead, which is fine since this screen only appears once per calendar choice.
- Passed cleanly, checked directly rather than assumed: no `NSAppTransportSecurity`/`NSAllowsArbitraryLoads` exceptions weakening TLS in `app.config.ts`; no custom `Linking`/deep-link handling in the app's own code that could act on attacker-controlled URL parameters (Supabase client has `detectSessionInUrl: false`).
- **Real finding, chased from an actual production log line, not a code-review guess — fixed, with a near-miss regression caught before it could ship**: checked real Vercel production logs (not just theory) and found a genuine Node deprecation warning on a live request — `url.parse()`, which Node's own message calls "prone to errors that have security implications." Traced it to source rather than dismissing it: `@supabase/node-fetch` (a transitive dependency this backend was stuck on via `@supabase/supabase-js@2.78.0`, `package-lock.json` never having advanced within the already-permitted `^2.78.0` range up to the real latest `2.116.0`) uses the legacy API internally. Confirmed the newer version line drops `@supabase/node-fetch` from the dependency tree entirely. Updating surfaced a real regression immediately, before it was ever committed: `createClient()` started throwing outright — `"Node.js 20 detected without native WebSocket support"` — because newer `@supabase/realtime-js` unconditionally constructs a `RealtimeClient` (this backend never calls `.channel()` anywhere, but the library gives no way to skip constructing one, only to configure it) and requires Node 22+'s native `WebSocket` or an explicit transport below that. This broke every test file and all local dev outright on this machine's Node 20.20.2 — Vercel production itself (already pinned to Node 24.x) wouldn't have hit it, but the fix needs to work locally too, not just in prod. Fixed by explicitly passing the `ws` package as the realtime transport (`lib/supabase.ts`) — exactly what `WebSocketLikeConstructor`'s own doc comment names as an intended, supported implementation; the one type cast needed is for a structural overload mismatch in `ws`'s own types, not a real incompatibility, confirmed by reading that interface directly rather than reaching for a blind `any`. Verified live in production after fixing, not just locally: deployed, confirmed the deployed `deploymentId` actually matched before trusting the result (the lesson from the webhook-cache-TTL mistake earlier this session), and pulled a real request's log line — empty `logs` array, deprecation warning gone.
- **Same staleness pattern found and fixed on the mobile side too**: `mobile/package.json` had the identical `@supabase/supabase-js@2.78.0` stuck in `package-lock.json`, never having advanced within its own already-permitted `^2.78.0` range. Updated the same way (`npm update`, landing on `2.109.0`, `@supabase/node-fetch` gone). Checked whether this mobile update would hit the same `RealtimeClient`/`WebSocket` regression just found and fixed on the backend, rather than assuming a different environment makes it moot: read `@supabase/realtime-js`'s actual `detectEnvironment()` logic — its very first check is `typeof WebSocket !== 'undefined'`, which React Native's environment satisfies unconditionally (a core global RN provides, not something that needs a version-gated native-vs-polyfill decision the way Node does) — so the Node-specific throw path can't trigger here. `expo-doctor`/typecheck both clean, no new issue. Pure-JS dependency change (no native module involved), so no `app.config.ts` version bump needed this time, unlike the three real native dependencies added earlier — not live-run-verified on a real device/Simulator (given this session's documented Simulator friction), but verified by tracing the actual conditional logic rather than assumed safe.
- The 11/5 `npm audit` warnings in `mobile/`/`backend/` (uuid buffer-bounds bug via `xcode`→`@expo/config-plugins`; `ajv`/`path-to-regexp`/`undici` via `@vercel/node`) are real CVEs but confirmed build-tooling-only — every `@vercel/node` import in `backend/` is `import type` (erased at compile time, never executed), and the vulnerable mobile chain only runs during `expo prebuild`, never shipped in the app bundle. `npm audit fix --force` in both cases suggests a major-version *downgrade* (Expo 46, `@vercel/node` 4.0.0) — do not apply it, it would break the app for no real security gain. Not re-litigating this without new evidence.
- **Real blocker for actual Canadian banks — code-side scaffolding now built and live-verified; the remaining gap is account actions, not code**: `linkTokenCreate` never set `redirect_uri`, and nothing handled an inbound OAuth redirect — this matters because most major Canadian banks (RBC, TD, Scotiabank, BMO, CIBC — all visible in this app's own institution search) commonly require OAuth-based Plaid Link. **Corrected a wrong assumption before writing any code**: initially planned to reuse this app's existing `txncalsync://` custom URL scheme with a `Linking` listener — checked Plaid's actual current OAuth docs first and that's wrong for iOS. OAuth-based Plaid Link on iOS requires a real Universal Link backed by Apple's Associated Domains system, not a custom scheme; the native SDK then handles resuming the Link session automatically once that's configured, no app-level deep-link listener/resume code needed at all (simpler than the custom-scheme approach would have been, once correctly understood).
  - `mobile/app.config.ts`: `ios.associatedDomains: ["applinks:backend-theta-fawn-72.vercel.app"]` — points at the existing backend deployment, no new domain needed. Verified via a real `expo prebuild` (not assumed) that this generates the correct `com.apple.developer.associated-domains` entitlement in the generated `.entitlements` file. Bumped `version` to `1.2.0` since this is a native/code-signing-level change, same reasoning as the earlier native-dependency version-bump fix.
  - `backend/.well-known/apple-app-site-association`: the verification file Associated Domains requires. **Real platform constraint hit and worked around**: Vercel's own docs state `/.well-known` paths can never be rewritten — only a real static file works there — so this is a literal file in the repo, not a route; `backend/vercel.json` (new file) only sets the `Content-Type: application/json` header Apple requires on it (extensionless files serve with a generic type otherwise) and separately rewrites the friendlier `/plaid-oauth-return` to a serverless function (allowed, since that path isn't under `/.well-known`). The file's `appID` has a clearly-marked placeholder Team ID (`TEAMID_PLACEHOLDER.com.rynjung1.transactioncalendarsync`) — genuinely can't be a real value until Apple Developer enrollment completes and provides one.
  - `backend/api/plaid-oauth-return.ts`: the actual `redirect_uri` target — Plaid's own docs describe this as simply "a blank web page you'll need to create and host"; in the success case iOS intercepts navigation to this path at the OS level and hands off to the native app before this page ever loads, so it only actually renders as a fallback (Associated Domains misconfigured, app not installed, opened directly in a browser) — says so plainly rather than being blank or broken.
  - `create-link-token.ts`: passes `redirect_uri` only when the new, optional `PLAID_REDIRECT_URI` env var is set (mirrors the required `PLAID_WEBHOOK_URL` pattern, but optional) — sending an *unregistered* redirect URI would make Plaid reject every Link session outright, not just OAuth ones, so this must stay unset until the Dashboard side is actually done.
  - **Verified live in production, not just typechecked**: both `/.well-known/apple-app-site-association` (real `200`, correct `content-type: application/json`, valid JSON, no redirect) and `/plaid-oauth-return` (real `200`, correct `content-type: text/html`, clean rewritten path, no redirect) confirmed directly against the deployed backend.
  - **Not fully verifiable from here, genuinely blocked on account actions**: a real Apple Developer Team ID (still pending enrollment — see the business-structure decision above) to replace the AASA placeholder, the redirect URI actually registered in the Plaid Dashboard's Allowed redirect URIs list, and a real device test through an actual OAuth-requiring bank. This closes the code-side gap; what's left is genuinely not something more code can solve.
- **Found, deliberately not fixed, long-term risk documented rather than silently carried**: `react-native-calendar-events` (the app's only calendar-write library, and the thing the whole product depends on) is confirmed upstream-abandoned, not just "untested against New Architecture" as previously recorded — `2.2.0` (pinned here) is both the last version ever published (2022-06-26) and the newest that has ever existed on npm, and the library's own GitHub repo states verbatim: "This repository is deprecated and no longer supported." It works today — proven by this session's own live, independently-verified end-to-end test (48/48 real calendar events, confirmed in the actual EventKit database) — but "works today" isn't the same guarantee as "has an upstream fix path if a future iOS release breaks it," because there isn't one without forking the library or replacing it. Not an immediate action item, but a real fact to know going into any future iOS SDK upgrade. A concrete consequence of the abandonment, not just a theoretical one: iOS 17 introduced a genuinely separate "write-only" calendar permission level (`EKEntityWriteOnlyAccessToEvents`), distinct from full read/write access — checked the library's actual native source (`ios/RNCalendarEvents.m`) and it only ever calls the older `requestAccessToEntityType:` API (pre-dating iOS 17's split, consistent with a 2022 last release), with no path to request write-only. Confirmed our own code never reads existing calendar content anywhere (`findCalendars()` only returns calendar metadata — id/title/source — never event contents; the only other calls are `saveEvent`/`removeEvent`, both writes), matching what the privacy policy already claims. So the app requests broader OS permission (full access) than it actually uses (write-only), purely because the dependency never adopted the newer, more privacy-respecting API — not fixable without replacing the library.
- **Resolves a previously-open question, no code change needed**: this file used to list "iOS deployment target vs. Plaid's podspec minimum" as unverified. Traced it via source rather than guessing: `app.config.ts`'s `expo-build-properties` config only sets `android.minSdkVersion`, so iOS falls through to React Native's own Podfile default — read directly from `node_modules/react-native/scripts/cocoapods/helpers.rb`, RN 0.86.3's floor is `15.1`, exactly matching Plaid's SDK podspec minimum. No conflict. Caveat: confirmed by reading source, not by an actual `pod install`/prebuild run — worth a quick empirical confirmation on the next real build, but nothing to change.

### Mobile release hygiene
- **Real gap, found and fixed before it could ever bite**: `app.config.ts`'s `version` stayed at `"1.0.0"` the entire session despite three real native dependencies being added (`expo-secure-store`, `react-native-webview` for Turnstile, `@sentry/react-native`) — none of their additions bumped it. This matters specifically because `runtimeVersion: { policy: "appVersion" }` (already set, for `expo-updates` OTA support) ties Expo Updates' native-compatibility fingerprint directly to this field — confirmed against Expo's own docs, not assumed: the `"appVersion"` policy reads this exact field, and Expo's documented practice is to bump it "just as you would for a user-facing release" whenever native code changes, specifically because forgetting means a JS-only OTA update could get delivered to an older installed binary that reports the same runtime version but actually lacks the native module the update's JS depends on. No live EAS Update channel is serving real users yet, so nothing broke from this in practice — bumped to `1.1.0` now, closing the gap before a real `eas update` could ever hit it.

### Deployment
- Backend is deployed to Vercel (project `ryan-95ce/backend`). Stable production URL: `https://backend-theta-fawn-72.vercel.app` — `mobile/app.config.ts`'s `extra.apiBaseUrl` points here, and `PLAID_WEBHOOK_URL` (set as a Vercel production env var, not in this repo) points to `<that URL>/api/plaid/webhook`.
- **`backend/package.json` now pins `engines.node` to `"24.x"`** — confirmed via `vercel project ls` that production is already running Node 24.x (Vercel's own docs: unpinned Functions use whatever the dashboard's Node.js Version setting says, which is `24.x` here, not something guaranteed to stay put — Vercel has separately announced Node 20 being deprecated on Vercel on 2026-10-01). Pinning makes the intended runtime explicit in-repo rather than depending on a dashboard setting nobody's looking at, and prevents a future silent major-version bump on some later deploy. Deliberately `"24.x"`, not a range like `>=20.0.0` — a range would just resolve to "whatever Vercel's latest is" at deploy time, which is the exact drift this exists to prevent; also deliberately not `"20.x"` (would actually *downgrade* current production, and walk straight into that same Node 20 deprecation). This does not affect local dev, which stays on Node 20.20.2 (`npm install` prints an advisory `EBADENGINE` warning, confirmed non-blocking — install/typecheck/test all still ran clean) — the pin only governs what Vercel deploys.
- All six required env vars (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `PLAID_CLIENT_ID`, `PLAID_SECRET`, `PLAID_ENV`, `TOKEN_ENCRYPTION_KEY`, `PLAID_WEBHOOK_URL`) are set on Vercel for the Production environment only — not Preview or Development. Add them there too if preview deploys or `vercel dev` need to hit real Plaid/Supabase.
- Redeploy (`vercel --prod`) after any env var change — existing warm function instances don't pick up new values without one.

## Key constraints
- User is targeting production App Store deployment, not just personal/dev use — so Plaid production access application (not just sandbox) will eventually be required. Sandbox/dev mode is fine for initial build.
- Bank is in Canada — confirm Plaid supports the user's specific institution before assuming full feature parity with US banks.
- Never store Plaid `access_token` unencrypted. Never generate `link_token` client-side.
- One calendar event per transaction (not daily digest) unless this changes — confirm before altering event granularity.
- Location on events: bank transaction data typically only gives merchant name, not GPS coordinates. Don't assume precise location is available — plan to use merchant name in the event title/location field rather than requiring geocoding, unless we explicitly decide to add a Places lookup step.
- **PIPEDA compliance** — researched directly against the OPC's own site (priv.gc.ca), not assumed. `PRIVACY.md` is the source-of-truth privacy policy text; published live at `https://claude.ai/code/artifact/4e325609-3c98-4037-a842-c39e4b7fce07` (linked from `AuthScreen.tsx`) — **that artifact is private by default and must be shared publicly (via the page's own share menu) before it can be used as the App Store Connect privacy policy URL; not done yet.** `DELETE /api/account` (backend) + Settings → Delete account (mobile) satisfy both PIPEDA's retention principle and App Store Guideline 5.1.1(v) — deployed and verified end-to-end with a real throwaway user (all four data stores confirmed empty afterward, and the Plaid item genuinely revoked, not just deleted locally).
- **Real disclosure gap, found on a fresh read-through and fixed**: the "Who else touches your information" section listed Plaid, Supabase, Vercel, and Apple/Google — but not Sentry or Cloudflare, both added earlier this session as real third-party subprocessors once actually enabled (a real DSN/site key configured). Neither is live yet (both are still empty placeholders), so there's no current live gap in practice — but the policy would have gone stale the moment either was turned on, silently sending real data (crash reports; a bot-check signal) to an undisclosed third party, a genuine PIPEDA problem and a likely App Store/Play Store privacy-label omission too. Fixed proactively, before either goes live: added both to `PRIVACY.md`, the bundled `mobile/assets/privacy-policy.html`, and republished the live artifact (still private, not yet shared — same caveat as above) — worded to make clear what's actually sent (crash/error diagnostics; an automated bot-check) and, just as importantly, what's explicitly never sent (bank login, Plaid access token, app password).
- **Terms of Service** — `TERMS.md` is the source-of-truth text (covers: not financial advice, no accuracy guarantee on synced data, calendar events aren't deleted by the app, acceptable use, disclaimer of warranties, limitation of liability, Canadian governing law); published live at `https://claude.ai/code/artifact/33d1e392-0850-42bf-b17f-75cc5456d2b9`, linked from `AuthScreen.tsx` next to Privacy Policy. **Same caveat as the privacy policy: private by default, needs the user to share it publicly via the page's own share menu before it's usable as a real, linkable terms URL (e.g. for Plaid's own application, or an App Store Connect field that asks for one).** Not reviewed by a lawyer — reasonable for a free, individual-operated app, but worth real legal review before this app has real monetization or meaningfully more users.

## Working style for this project
- Confirm before writing/changing code — do not just implement without checking in first, especially for architecture-level decisions (data models, auth flow, token handling).
- When claims are made about test results, working code, or fixes, they need real pasted evidence (actual output, not a summary) before being treated as verified.
- A single test case or single transaction/event tested successfully is not sufficient to declare a feature working project-wide — needs testing across multiple transaction types/edge cases before being called done.

## Open questions / not yet decided
- Exact Supabase schema may evolve beyond `0001_init.sql` as features are added
- ~~Whether to support multiple linked bank accounts per user~~ — decided: yes, at launch. Backend already supported it (one `plaid_items` row per item, all synced by `/api/plaid/sync`); mobile now has an actual entry point too (`SettingsScreen` → "Add another bank account" → `LinkAccountScreen` with its new `onCancel`, gated by a separate `addingAccount` flag in `App.tsx` since there's no real navigation stack to route a second link flow through).
- ~~Whether categories get surfaced as calendar event color-coding~~ — decided: no. Category stays in the event notes field only (`mobile/src/lib/calendar.ts`); not worth the added complexity, and calendar apps have limited/inconsistent color palettes to map 16 categories onto anyway.
- ~~Development environment currently runs on Node 18~~ — resolved, confirmed Node 20.20.2 in the active dev environment now.
- **Fixed for real on the second attempt — the first one (a plain PFC-only refactor) was verified to change nothing**: a transaction with no PFC enrichment now falls back to `backend/lib/legacyCategoryMapping.ts`, generated from Plaid's own published legacy-category → PFC mapping (242 entries, fetched directly rather than guessed from memory). Deliberately includes only the 176 of 242 entries that are unambiguous at the PFC `primary` level — the other 66 map one legacy category to multiple possible primaries, and guessing among them was rejected since a wrong exclusion is worse than the prior gap. A `category_id` outside this map (unmapped or ambiguous) still can't be excluded by category, same as before — that residual limitation is accepted, not a bug. Verified with a real test: the specific failing case from the first attempt (no PFC, `category_id` unambiguously mapping to an excluded value) now actually flips from synced to excluded, plus confirmed no wrong guesses on ambiguous/unknown `category_id`s and no regressions on real-PFC or amount filtering.
- **N+1 `sync_filters` fetch — fixed**: `syncPlaidItem()` now takes `filters` as a parameter instead of looking them up itself; `getSyncFilters(userId)` (`backend/lib/plaidSync.ts`) is called once per request by each caller (`sync.ts` fetches once before looping all of a user's items, skipped entirely if there are none; `webhook.ts` and `exchange-token.ts` fetch once for their single item). Ready for whenever multi-account is decided, doesn't depend on that decision.
- ~~Not yet actually verified at runtime~~ — done, see the runtime-verified entry under Mobile UI/UX findings above (real Plaid Link session through to real calendar events, independently confirmed in the EventKit database). The Metro OOM issue under multiple concurrent Claude Code sessions is still real and unresolved as a dev-environment annoyance (worked around this time by testing via `xcrun simctl`/device logs instead of live Metro), and the `expo-updates` deep-link crash in `expo-dev-launcher` (`EXDevLauncherUpdatesHelper createUpdatesConfigurationWithURL:...`) is still unfixed and unpursued — neither blocks shipping, just dev convenience.
- **Dev-environment incident, not an app bug**: `mobile/node_modules` was found completely empty mid-session (its own Metro process had also died), during a stretch where this same machine hit genuinely critical low disk space (down to ~340MB free at one point, later an outright `ENOSPC`) while multiple concurrent Claude Code sessions were also active — the same resource-contention conditions the Metro OOM note above already flags. Checked the actual command history first rather than guessing: no `rm -rf` or install/prune command against `mobile/node_modules` appears anywhere in this session. `git status` showed zero uncommitted source changes and `npm install` reproduced `package-lock.json` byte-for-byte (confirmed via `git diff`), so nothing was actually lost — `node_modules` is never committed and is fully regenerable — but worth knowing this machine can silently drop a project's entire `node_modules` under disk/memory pressure, not just fail loudly.
- ~~Still genuinely untested live~~ — **done, third attempt succeeded end-to-end**. First two attempts documented the road here: attempt one abandoned to Simulator friction; attempt two found the installed dev-client binary had gone stale (predated `expo-secure-store`, crashed with `Cannot find native module 'ExpoSecureStore'`). This time: ran a real `expo prebuild` + `xcodebuild` (`expo run:ios`'s own device detection got confused by this sandbox's host Mac appearing in `xctrace`'s device list and insisted on physical code-signing — worked around by driving `xcodebuild` directly against the Simulator destination) — **BUILD SUCCEEDED**, installed the fresh binary, confirmed it launches clean with all of this session's native additions actually compiled in. Then a real, full live pass with the user tapping through on real hardware while I verified state: sign-in (had to create the test user via the admin API after hitting Supabase's real email-confirmation rate limit from this session's own testing), a genuine Plaid Link session against a real sandbox institution (Platypus Bank RUX Auth), a real sync writing transactions to the calendar, and — the actual target of this check — the Insights tab's chart rendering correctly with real data: a real bar chart from `react-native-gifted-charts`, correct axis/day labels, an average reference line, and a category breakdown. This also visibly reconfirmed the CAD currency fix live for the first time (every figure showed `CA$`, e.g. `CA$7,064.63` total spend), not just via the earlier `Intl.NumberFormat` unit test. `react-native-calendar-events` and `react-native-plaid-link-sdk` are both now proven working on a build that reflects the *current* state of every native dependency this session touched, not just the original pre-session build.
